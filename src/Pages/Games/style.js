import styled from "styled-components";

export const Container = styled.div`
    main{
        padding-top: 50px;
        h2.main--title{
            color: var(--purple);
            font-family: var(--blackOpsFont);
            font-weight: 500;
            font-size: 36px;
        }
    }
`