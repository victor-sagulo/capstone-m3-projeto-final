import styled from "styled-components";

export const Container = styled.div`
    div.home--bottom{
        margin-bottom: 100px;
        h2.main--title{
            color: var(--purple);
            font-family: var(--blackOpsFont);
            font-weight: 500;
            font-size: 36px;
        }
    }
`